{
    "DBInfo":
    {
        "Name": "PostGresConnect",
        // PostGres doesn't use the "SslMode" property, make sure to remove it or the connection will be refused

        ////// CHANGE YOUR DATABASE NAME!!! Then uncomment below line...
        "ConnectionString": "server=localhost;userId=postgres;password=root;port=5432;database=cSharpTestDB;"
    }
    }
